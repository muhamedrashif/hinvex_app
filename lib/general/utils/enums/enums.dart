enum ConstructionStatus { newLaunch, readyToMove, uderConstruction }

enum SelectedCategory {
  house,
  apartments,
  landsPlots,
  commercial,
  coWorkingSoace,
  pGGuestHouse
}

enum SelectedType { sell, rent }

enum SelectedFurnisher { furnished, semiFurnished, unFurnished }

enum SelectedListedBy { builder, dealer, owner }
